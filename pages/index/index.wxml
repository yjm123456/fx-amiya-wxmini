
<view class="container">
    <!-- 轮播图 -->
    <swiper class='banner' indicator-dots='true' autoplay='true' interval='5000' duration='1000' indicator-active-color="#DEC350">
        <swiper-item wx:for="{{carouselImage}}" wx:key="picUrl" bindtap="redirect" data-url="{{item.linkUrl}}">
            <image src='{{item.picUrl}}' class='slide' mode='aspectFill'></image>
        </swiper-item>
    </swiper>
    <!-- 咨询 -->
    <!-- <view class="consulting_service">
    <image class="img" src="/images/amy.png"></image>
    <view class="consulting_center">
      <view class="h3">客服咨询</view>
      <view class="h4">美容整形医院项目咨询</view>
    </view>
    <view>
      
    </view>
    <button class='customerService' hover-class="none" bindcontact="handleContact" openType="contact">
      <view class="service">立即咨询</view>
      
		</button>
  </view> -->
    <!-- 项目分类 -->
    <!-- <view class="project_con" wx:if="{{projectArr.length}}">
    <view class="title_h3">项目分类</view>
    <view class="con">
      <block wx:for="{{projectArr}}" wx:key="index">
        <view class="item" bindtap="projectClass">
          <van-image width="30" height="30" src="{{item.img}}"/>
          <view class="text">{{item.name}}</view>
        </view>
      </block>
    </view>
  </view> -->
   <!-- 公告 -->
   <view class="notice">
    <van-notice-bar
  left-icon="volume-o"
  scrollable="{{ true }}"
  color="#DEC350" 
  background="#262626" 
  text="啊美雅MEIYA卡即将上线,敬请期待"/>
   </view>
    <!-- 活动 -->
    <view class="activity">
        <view class="activity_item" bindtap="handleActivity" data-activity="{{1}}">
            <image src="../../images/hyfl.jpg" mode="heightFix"></image>
        </view>
        <view class="activity_item" bindtap="handleActivity" data-activity="{{2}}">
            <image src="../../images/yqxyh.jpg" mode="aspectFill"></image>
        </view>
    </view>
    <!-- 导航栏二 -->
    <view class="navigation_list">
        <view class="grid_item" data-url="/pages/meiYaCard/meiYaCard" bindtap="to"><text>MEIYA卡</text></view>
    <view class="grid_item" data-url="/pages/shoppingMall/shoppingMall" bindtap="toShop"><text>MEIYA甄选</text></view>
    <view class="grid_item" data-url="/pages/beautyDiary/beautyDiary" bindtap="to"><text>美学分享</text></view>
    </view>

    <!-- 商品列表 -->
    <view class="product_list">
        <view class="h3 title">猜你喜欢</view>
        <view class="product_warp">
            <view class="product" wx:for="{{goodsList}}" bindtap="goodsDetails" data-goodsid="{{item.id}}" wx:key="index">
                <view wx:if="{{item.isMember}}" class="tag">会员专享</view>
                <view>
                    <view class="product_top">
                        <image src="{{item.thumbPicUrl}}" mode="widthFix"></image>
                    </view>
                    <view class="product_bottom">
                        <view class="product_name">{{item.name}}</view>
                        <view class="price">
                            <text class="present_price">¥{{item.minShowPrice}}</text>
                            <text class="original_price">¥{{item.maxShowPrice}}</text>
                        </view>
                    </view>
                </view>
            </view>
        </view>
    </view>
</view>
<!-- 授权手机号 -->
<authPhone control="{{controlAuthPhone}}" bind:successBindPhone="successBindPhone" bind:cancelBindPhone="cancelBindPhone"></authPhone>
<!-- 分享弹窗 -->
<van-dialog custom-class="van_dialog_action" use-slot id="van-dialog">
    <view class="share">
        <view class="share_by">{{nickname}}分享给您一张抵用券:</view>
        <view class="content" >
            <view class="item" >
                <view class="item_top">
                    <view class="item_name">{{vouchername}}</view>                   
                    <view class="item_price">¥{{vouchermoney}}</view>
                </view>
            </view>
        </view>
    </view>
</van-dialog>