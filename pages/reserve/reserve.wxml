<header itemInfo="{{itemInfo}}" />

<reserveInfo appointmentDate="{{appointmentDate}}" hospitalId="{{hospitalId}}" itemInfoId="{{itemInfoId}}" time="{{time}}" week="{{week}}" currentDate="{{currentDate}}" currentWeek="{{currentWeek}}" currentTime="{{currentTime}}" />